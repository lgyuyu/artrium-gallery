generator client {
  provider = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
}

datasource db {
  provider = "sqlite"
  // 构建时如果 DATABASE_URL 没设置，用这个占位（driver adapter 模式下 schema 的 url 不会被直接使用）
  url      = env("DATABASE_URL")
}
