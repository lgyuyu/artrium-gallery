# Turso/libSQL 数据库连接（从 Turso 控制台获取）
# 本地开发：也可以用本地 SQLite 文件，如 file:./dev.db
DATABASE_URL=libsql://your-db.turso.io

# Turso Auth Token（仅 libsql:// 地址需要，本地 file: 不需要）
DATABASE_AUTH_TOKEN=your-token-here
